const express = require ('express');

const app = express() ;


// To dos : node is not installed need to install it
app.listen(3000, ()=>{ console.log("Server is running on Port 3000")});


// making these changes to work with revert reset and stash commands
